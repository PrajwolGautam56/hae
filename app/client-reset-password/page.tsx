"use client";
import { useEffect,useMemo,useState } from "react";
import { createClient } from "@supabase/supabase-js";

export default function ClientResetPassword(){const supabase=useMemo(()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!),[]);const [ready,setReady]=useState(false);const [message,setMessage]=useState("Verifying secure link…");const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [busy,setBusy]=useState(false);useEffect(()=>{void(async()=>{const params=new URLSearchParams(window.location.search);const tokenHash=params.get("token_hash");if(tokenHash){const {error}=await supabase.auth.verifyOtp({token_hash:tokenHash,type:"recovery"});if(error){setMessage(error.message);return}setReady(true);setMessage("Link verified. Choose your customer portal password.");window.history.replaceState({},"","/client-reset-password");return}const {data}=await supabase.auth.getSession();if(data.session){setReady(true);setMessage("Link verified. Choose your customer portal password.")}else setMessage("This password link is invalid or expired.")})()},[supabase]);async function save(){if(password.length<8)return setMessage("Use at least 8 characters.");if(password!==confirm)return setMessage("Passwords do not match.");setBusy(true);const {error}=await supabase.auth.updateUser({password});setBusy(false);if(error)return setMessage(error.message);await supabase.auth.signOut();setReady(false);setMessage("Password saved. You can now sign in to the customer portal.")};return <main className="client-login-page"><section className="client-login-card"><div className="client-login-brand"><img src="/hamro-afno-logo.jpeg" alt="Hamro Afno Enterprises"/><span><strong>Customer portal</strong><small>Secure password setup</small></span></div><div className="client-login-copy"><small>ACCOUNT SECURITY</small><h1>Choose your password</h1><p>{message}</p></div>{ready&&<><label>New password<input type="password" minLength={8} value={password} onChange={event=>setPassword(event.target.value)}/></label><label>Confirm password<input type="password" minLength={8} value={confirm} onChange={event=>setConfirm(event.target.value)}/></label><button className="client-login-button" disabled={busy||!password||!confirm} onClick={save}>{busy?"Saving…":"Save password"}</button></>}<a className="client-login-link" href="/client-login">Return to customer login</a></section></main>}
