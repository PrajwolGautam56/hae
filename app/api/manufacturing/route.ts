import { NextResponse } from "next/server";
import { getSupabaseAdmin } from "../../../lib/supabase-server";
export const dynamic = "force-dynamic";

async function company(db:any){const {data,error}=await db.from("companies").select("id").order("created_at").limit(1);if(error||!data?.[0])throw error||new Error("Company not found");return data[0]}
export async function GET(){try{const db=getSupabaseAdmin();if(!db)throw new Error("Supabase configuration is missing");const c=await company(db);const {data,error}=await db.from("production_batches").select("id,batch_no,production_date,output_quantity,notes,products!production_batches_output_product_id_fkey(name,unit),production_consumptions(quantity,products(name,unit))").eq("company_id",c.id).order("production_date",{ascending:false}).limit(30);if(error)throw error;return NextResponse.json({batches:data||[]})}catch(error:any){return NextResponse.json({error:error?.message||"Manufacturing error"},{status:500})}}
export async function POST(request:Request){try{const db=getSupabaseAdmin();if(!db)throw new Error("Supabase configuration is missing");const c=await company(db);const body=await request.json();const {data,error}=await db.rpc("record_production_batch",{p_company_id:c.id,p_fiscal_year_id:body.fiscalYearId,p_date:body.date,p_output_product_id:body.outputProductId,p_output_quantity:Number(body.outputQuantity),p_consumptions:body.consumptions,p_notes:String(body.notes||"")});if(error)throw error;return NextResponse.json(data,{status:201})}catch(error:any){return NextResponse.json({error:error?.message||"Could not record production"},{status:500})}}
