"use client";
import { useState } from "react";
export default function ForgotPassword(){const [email,setEmail]=useState("prajwolgautam56@gmail.com");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function send(){setBusy(true);setMessage("");const response=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const data=await response.json();setBusy(false);setMessage(data.message||data.error||"Could not send email")}return <main className="login-page"><section className="login-card"><div className="login-brand"><img src="/hamro-afno-logo.jpeg" alt="Hamro Afno Enterprises"/><div><strong>Hamro Afno Enterprises</strong><span>Secure account recovery</span></div></div><div className="login-copy"><small>PASSWORD RECOVERY</small><h1>Reset password</h1><p>Enter your assigned team email. We will send a secure password-reset link.</p></div><label>Email address<input autoFocus type="email" value={email} onChange={e=>setEmail(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} autoComplete="email"/></label>{message&&<div className="reset-message">{message}</div>}<button className="primary" disabled={busy||!email} onClick={send}>{busy?"Sending email…":"Email reset link"}</button><a href="/login">← Return to sign in</a><footer>For security, reset links expire and can only be used once.</footer></section></main>}
